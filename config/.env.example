# Airリザーブ自動予約システム - 環境変数設定例
# このファイルをコピーして .env ファイルを作成し、実際の値を設定してください
# cp config/.env.example .env

# ============================================
# 予約者情報（必須）
# ============================================

# 予約者（保護者）の氏名
BOOKER_NAME=山田太郎

# 予約者（保護者）のフリガナ（姓）
BOOKER_NAME_KANA=ヤマダ

# 予約者（保護者）のフリガナ（名）
BOOKER_NAME_KANA_MEI=タロウ

# 連絡先メールアドレス
BOOKER_EMAIL=example@example.com

# 連絡先電話番号（ハイフンありなしどちらでも可）
BOOKER_PHONE=090-1234-5678

# お子様の氏名
CHILD_NAME=山田花子

# お子様の年齢（0-6歳）
CHILD_AGE=2

# ============================================
# 予約設定（必須）
# ============================================

# 予約ページのURL（直接指定する場合）
# TARGET_URL=https://airrsv.net/kokoroto-azukari/calendar

# サイトモード（TARGET_URLが未設定の場合のみ有効）
# production: 本番サイト（デフォルト）
# test: テストサイト
SITE_MODE=production

# 次回予約公開日時（YYYY-MM-DD HH:MM:SS形式）
NEXT_RELEASE_DATETIME=2024-11-01 09:30:00

# 監視時間（分）
MONITOR_DURATION_MINUTES=10

# 希望曜日（カンマ区切り）
PREFERRED_DAYS=月,水,金

# 希望開始時間（HH:MM形式）
PREFERRED_TIME_START=09:00

# 希望終了時間（HH:MM形式）
PREFERRED_TIME_END=17:00

# ============================================
# 実行モード（推奨設定）
# ============================================

# テストモード（true: 予約を実行しない、false: 実際に予約を実行）
DRY_RUN=false

# ヘッドレスモード（true: ブラウザを表示しない、false: ブラウザを表示）
HEADLESS=true

# デバッグモード（true: 詳細ログを出力、false: 通常ログ）
DEBUG=false

# テストサイトモード（14日前チェック機能を有効にする場合）
# TEST_SITE_MODE=false

# 最終送信前に停止（true: 送信前に停止、false: 実際に送信）
# デフォルト値はtrue（安全のため）です。本番実行時のみfalseに設定してください
# フォーム入力テストなどで使用
STOP_BEFORE_SUBMIT=true

# 手動確認が必要（true: 確認画面で手動確認、false: 自動実行）
REQUIRE_MANUAL_CONFIRMATION=false

# ============================================
# 通知設定（オプション）
# ============================================

# 予約成功時の通知
NOTIFY_SUCCESS=true

# 予約失敗時の通知
NOTIFY_FAILURE=true
